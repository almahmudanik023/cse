#include<stdio.h>
#include<math.h>

int main()
{
    float x, root;
    printf("Enter the value of x: ");
    scanf("%f",&x);

    root = sqrt(x);

    printf("The value of square root of x is %.4f", root);

}